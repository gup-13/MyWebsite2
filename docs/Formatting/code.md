---
title: Code
layout: default
nav_order: 1
parent: Formatting
---

# Writing Code

This is a codeblock:
```
if (codeblock)
    write code
```

It can be language specific:
```c++
if (codeblock == true)
{
    doSomething();
}
```

This is a `line of code`.

